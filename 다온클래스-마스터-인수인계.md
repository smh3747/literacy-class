# 다온클래스 마스터 인수인계

> 기준: **HEAD = `4bb6af7` (step275)** · 갱신 2026-06-29
> 스냅샷 4개(`_snapshot/SNAPSHOT-{pages,components,lib,migrations}.md`)는 `node scripts/make-snapshot.js`로 재생성. (pages 46 / components 24 / lib 23 / migrations 76)

## §1. 현황 머리말
- 초등(주로 5학년) AI 글쓰기 피드백 시스템. 교사가 주제 등록 → 학생이 글 작성 → Gemini가 채점·피드백. 학급별 분리 운영. UI·주석·커밋은 한국어.
- Next.js 14(Pages Router)·React 18·Tailwind·Supabase(Auth+Postgres)·Gemini·Vercel. 테스트 프레임워크 없음 → 검증은 `npm run build` + 수동 시연.
- 최근 작업 흐름: **맞춤법 표시 정확도/안내(step270~275)** 와 **주제 공유 네트워크 토대(step272)**.

## §2. 제품 정체·철학
- **무엇**: 학급 단위로 완전 분리된, 교사 주도 AI 글쓰기 피드백 도구. 프롬프트 IP는 서버 전용(`lib/prompts.server.js`), 모델명은 외부에 'AI'로만 표기.
- **존재 이유(뿌리)**: 아침 주제글쓰기 검사에 교사가 **50분~1시간** 소요 → 피드백이 위축·형식화됨. 이를 막아 **'내실 있는 피드백 + 교사 편의'** 둘 다 확보하는 것이 제품의 뿌리. (수익화는 그 위에 얹은 비즈니스 모델일 뿐, 핵심 가치가 아님.)
- **원칙**: 학생 대면 문구·AI 피드백은 항상 존댓말·쉬운 말·격려 톤. 맞춤법은 'AI 보조(완벽하지 않음)'로 안내하고 최종 판단은 교사 몫.

## §3. 스텝 이력 (최근)
| step | 해시 | 요약 |
|---|---|---|
| 275 | `4bb6af7` | koreanRules에 **'입니다/습니다 앞 띄어쓰기'** 규칙 추가("20일 입니다"→"20일입니다"). |
| 274 | `da1d08c` | 교사 맞춤법 배너 문구 부드럽게 + 글씨 키움(`text-sm`) + **'AI 모델 자동전환' 안내 4곳 제거**(api-key-guide·teacher/help·teacher/index 배너 2개). |
| 273 | `2e7fecf` | 학생 맞춤법 AI 보조 **안내문구** + 교사 **재평가 배너** + **무의미 교정 제거** 필터. |
| 272 | `f7a8989` | 공유 주제 **가져오기 출처 기록**(`topic_copies` + RLS + `topic_copy_counts()`), 화면 변화 없음. |
| 271 | `97f421d` | 재평가 경로 **mergeCorrections 통일** + **'번째' 띄어쓰기** 규칙. |
| 270 | `cb974f6` | 맞춤법 빨간줄 누락 수정 — **공백 허용 매칭**(`findOriginalRange`) + 저장 시 **original 보정**(`snapOriginalToEssay`). |
| 267 | `2b6b149` | (이 흐름 직전 베이스) 교사 제출현황 행 배치 개선. |

## §4. 코드 지도 (맞춤법·공유 핵심)
### lib/koreanRules.js
| 함수 | 줄(근사) | 역할 |
|---|---|---|
| `findRuleBasedErrors(text)` | `:10` | 정규식 규칙 보강: 문장부호 뒤 띄어쓰기, 할수있다, 안/않, 되/돼, ㄹ께요, **번째**(step271), **입니다/습니다**(step275), COMMON_TYPOS 등. |
| `findOriginalRange(essay, original)` | `:266` | 정확일치 실패 시 공백(`\s`+전각공백) 무시 매칭 → 본문 실제 구간 인덱스. `{start,end,exact,ambiguous}`/`null`. |
| `snapOriginalToEssay(c, essay)` | `:299` | AI original을 공백무시로 **유일 매칭**될 때만 본문 실제 문자열로 스냅(correction·reason 불변). |
| `mergeCorrections(aiCorr, essay)` | `:308` | 안/않 필터 → snap → `findRuleBasedErrors` 병합(중복 회피) → **무의미 교정 필터**(`original.trim()===correction.trim()` 제거) 후 반환. |
- 무의미 교정 필터(step273): mergeCorrections 끝 `return ai.filter(...)` (≈`:336-344`).
- `mergeCorrections`를 타는 경로(전부 규칙 보강 적용): 첫글·수정본(`pages/student/index.js`), **재평가**(`lib/regrade.js:125`, step271 통일), 맞춤법 일괄보강(`pages/teacher/grammar-backfill.js`).

### lib/notices.js — 상수 2개(현행, step274 반영)
```js
export const GRAMMAR_NOTICE_STUDENT =
  '✏️ 맞춤법 표시는 AI가 도와주는 거예요. 가끔 놓치거나 잘못 짚을 수 있으니 참고만 하고, 헷갈리면 선생님께 물어봐요.'
export const GRAMMAR_NOTICE_TEACHER =
  '맞춤법·띄어쓰기 표시는 AI가 도와드린 거예요. 완벽하진 않아 가끔 놓치기도 하니, 마지막엔 선생님께서 한 번 봐주시면 좋아요. 결과가 아쉬우면 아래에서 다시 검사할 수 있어요.'
```

### 교사 재평가 배너 / 학생 안내 (step273~274)
- 배너: `pages/teacher/submissions.js` 맞춤법 배지 직후 amber 배너, `<p className="text-sm ...">{GRAMMAR_NOTICE_TEACHER}</p>`(step274 text-sm), CTA `onClick={() => regradeOne(s, selectedStudent.profile.realname)}`("🔄 다시 평가하기", `disabled={regrading===s.id||bulkRegrading}`). 기존 "🔄 이 글 다시 평가" 버튼(≈`:1207`)도 유지 — 둘 다 같은 `regradeOne`(`:424`). 함수 자체 미수정.
- 학생: `pages/student/index.js` 첫 글 피드백 + `components/StudentFeedbackCard.js` 맞춤법 목록 아래 `GRAMMAR_NOTICE_STUDENT`. 학생엔 재평가 버튼 없음(권한). 카드는 admin도 공용.

### topic_copies (step272, 공유 가져오기 추적)
- 컬럼: `id, source_log_id(→topic_suggestion_logs), source_index, copied_by_teacher_id(→profiles), copied_topic_id(→topics, nullable), copied_at` + `UNIQUE(source_log_id, source_index, copied_by_teacher_id)`.
- RLS: `tc_insert_own`(본인 INSERT), `tc_select_own_or_admin`(본인 행+admin). 집계 `topic_copy_counts()`(SECURITY DEFINER, `n_teachers=COUNT(DISTINCT copied_by)`만 노출).
- INSERT 코드: `pages/teacher/topics.js:527` 신규 topic 등록 성공 블록, `copiedSource?.logId` 게이트, 실패는 무시(등록 흐름 유지). 가져온 주제는 **독립 유지**(원본 재연결 안 함).
- ⚠️ DB 객체는 Supabase 수동 실행 전제.

### 휴면 코드 — grammar 모델 체인
- `lib/gemini.js:142` `grammar:[...]`(채점 메인과 다른 모델 우선), 라우팅 `pages/api/ai.js:220` `grammarOnly→taskType:'grammar'`.
- 자동 채점엔 **미연결**(step268→269 롤백). 유일 사용처 = 교사 수동 '맞춤법 일괄보강'(`grammar-backfill.js`). 향후 "학생 요청→교사 승인 재검사"용 대기.

## §5. 확정 사실
- **폴백 채점은 거의 일어나지 않음**(교사별 무료 한도가 학급 단위엔 충분). 따라서 **채점 누락·점수 변동은 폴백 탓이 아니라 AI 자체의 변동성**이다.
- 그 변동성은 `koreanRules` 규칙으로 **'모양이 일정한 패턴'만** 보강 가능(예: 입니다/습니다, 번째, .그래서). **임의 띄어쓰기/문맥 의존 오류는 규칙으로 못 잡음** — AI 담당.
- 맞춤법 표시 누락의 과거 원인은 AI 누락이 아니라 **렌더러 정확일치 실패**였고 step270(공백 허용 매칭)+무의미 필터(step273)로 표시/데이터 정합 개선됨.
- 프롬프트 IP 보호: `lib/prompts.server.js`는 클라이언트 import 금지(서버 전용).

## §6. 다음 작업
- **A1 (현재 1순위) — 손으로 만든 주제도 공유 풀에 올리기.** 현재 공유는 `topic_suggestion_logs`(AI 추천)에만 묶여 있어, 교사가 직접 타이핑한 주제는 공유 불가. 이를 푸는 작업. **지금 plan mode 분석 진입 중.**
- **A2 (후순위) — "N명 사용" 배지 + 인기순 정렬.** `topic_copies`/`topic_copy_counts()` 기반. **A1 선행 필요 + 사용 데이터 1~2주 누적 대기**(현재 표본 0~소량이라 정렬 의미 약함).
- (참고) grammar 체인 활용 "학생 요청→교사 승인 맞춤법 재검사"는 별도 트랙.
